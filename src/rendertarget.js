var RenderTarget = function() {
  var self = this;

  


};

RenderTarget.CreateFromDefaults = function(gl) {
  return new RenderTarget(gl);
};

RenderTarget.CreateFromScratch = function(gl) {
  return new RenderTarget(gl);
};
